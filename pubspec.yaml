name: toque_musical
description: "Aplicativo para controle de presença de alunos e acompanhamento do progresso acadêmico"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  package_info_plus: ^8.1.3
  path: ^1.9.0
  rxdart: ^0.28.0
  sqflite: ^2.4.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
